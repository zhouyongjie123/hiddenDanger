<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zyj.hiddendanger.flow.mapper.ApprovalFlowNodeMapper">

    <resultMap id="BaseResultMap" type="com.zyj.hiddendanger.model.service.flow.approval.domain.node.ApprovalFlowNode">
        <id property="id" column="id"/>
        <result property="status" column="status"/>
        <result property="approverId" column="approver_id"/>
        <result property="deleted" column="is_deleted"/>
        <result property="creatorId" column="creator_id"/>
        <result property="updaterId" column="updater_id"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>
    <insert id="insertBatch">
        INSERT INTO approval_flow_node(id, status, approver_id, is_deleted, creator_id, updater_id, create_time,
        update_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.id}, #{item.status},#{item.approverId}, #{item.deleted}, #{item.creatorId}, #{item.updaterId},
            #{item.createTime},
            #{item.updateTime})
        </foreach>
    </insert>
</mapper>
